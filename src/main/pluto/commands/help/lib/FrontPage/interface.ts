import EmbedInterface from "../../../../embeds/EmbedInterface";

const HelpFrontPageInterface = new EmbedInterface()

HelpFrontPageInterface
    .setTitle('Help')
    .setDescription('Get help with all things \'Pluto\'.')
    .setColor('#DB4848')

export default HelpFrontPageInterface